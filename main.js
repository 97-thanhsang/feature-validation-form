function Validator(options){
    var formElement = document.querySelector(options.form);
    if (formElement) {
        
    }
}

Validator.isRequied = function () {
    return 1;
}
Validator.isEmail = function () {
    return 2;
}